# План доработки сценария «Убийство в таверне»

## 1. Утверждение канона (Story Decisions)

> Эти решения должны быть приняты автором сценария до наполнения JSON-данных.

- [ ] **Выбор нанимателя Детектива**
    - [ ] Оставить `primary` (Шериф) как единственного канонического заказчика.
    - [ ] Или утвердить `alternative` (Художник) как скрытого заказчика.
    - [ ] Зафиксировать выбор в `characters/detective.json` (поле `investigation.employer`).
- [ ] **Механика прибытия Детектива**
    - [ ] Выбрать 1 из 4 вариантов из README (жертва связана с таверной / след ведёт к таверне / случайный визит /
      наводнение как триггер).
    - [ ] Описать этот механизм в `README.md` или в `events.json` (стартовый event).
- [ ] **Призрак (dead_wife) как possible_killer**
    - [ ] dead_wife — `ghost` с `can_be_murder_victim: false`, но указан в `possible_killers` всех 6 матриц
      `murder_matrix.json`.
    - [ ] Решить: оставить как возможного убийцу (призрак может совершить убийство в возможном сценарии) или убрать из
      `possible_killers` (тогда dead_wife — только улика/призрак, не убийца).

## 2. Наполнение данных (Data Filling)

> Заполнение полей в JSON-файлах, которые сейчас содержат `null` или `needs_definition`.

- [ ] **Личности персонажей**
    - [ ] Придумать и вписать имена для 6 основных NPC (Трактирщик, Моряк, Официантка, Врач, Детектив, Художник).
    - [ ] Придумать и вписать имена для контекстных NPC (Шериф, Капитан корабля, Мэр) и призрака (Мёртвая жена).
    - [ ] Проверить, чтобы имена не конфликтовали с историческими/публичными персонажами.
- [ ] **Название корабля**
    - [ ] Придумать название судна Моряка/Капитана.
    - [ ] Вписать в `characters/sailor.json` или `characters/ship_captain.json` (поле `vessel_name` или аналог).
- [ ] **Дополнительные связи**
    - [ ] **Моряк:** Определить, есть ли у него дополнительные тайны/мотивы, помимо текущих. Записать в
      `characters/sailor.json`.
    - [ ] **Художник:** Определить его дополнительные жертвы или мотивы. Записать в `characters/artist.json`.
- [x] **Статус `ghost` (призраки)**
    - [x] Добавлен в `characters.schema.json` (enum `status` + description).
    - [x] `dead_wife.json` перенесён с `main_npc` на `ghost`; расписание, organizations, can_be_murder_victim: false.
    - [x] Задокументирован в `README.md` (разделы «Призраки», «Расписание дня», «Механика d6»).

## 3. Детализация улик и матриц (Evidence & Matrix)

- [ ] **Содержимое улик**
    - [ ] Пройтись по `evidence.json`.
    - [ ] Заполнить поле `content`/`description` для каждой улики (текст письма, описание пятна, записная книжка и
      т.д.).
    - [ ] Проверить, что у каждой улики есть `interpretation` (как её понимают персонажи) и `false_leads` (если
      применимо).
- [ ] **Матрицы убийства**
    - [ ] Пройтись по `murder_matrix.json`.
    - [ ] Для каждой жертвы (d6) проверить:
        - [ ] Заполнены ли `false_leads` (ложные следы).
        - [ ] Связаны ли `required_evidence` с ID улик из `evidence.json`.
        - [ ] Есть ли `special_consequences` (что меняется в мире, если эта жертва убита).

## 4. Техническая валидация (Tech Check)

- [ ] **JSON Schema Validation**
    - [ ] Запустить валидацию всех файлов `characters/`, `locations/`, `organizations/` по их `*.schema.json`.
    - [ ] Исправить ошибки, если структура JSON не соответствует схеме.
- [ ] **Согласованность расписаний**
    - [ ] Проверить `daily_schedule` всех основных персонажей.
    - [ ] Убедиться, что никто не находится в двух локациях одновременно (конфликт `time` + `location`).
    - [ ] Проверить, что контекстные NPC имеют `daily_schedule: null` или только `daily_schedule_note`.
    - [ ] Убедиться, что призраки (`status: ghost`, напр. dead_wife) имеют `can_be_murder_victim: false`, но могут иметь
      `daily_schedule` и `organizations`.
- [ ] **Изоляция и локации**
    - [ ] Убедиться, что все `gameplay`-локации имеют `parent_location: "tavern"`.
    - [ ] Проверить, что нет `gameplay`-локаций, которые игроки могут покинуть до конца сценария.
- [ ] **Статусы жертв**
    - [ ] Проверить, что у контекстных NPC (Капитан, Мэр), активного-но-не-жертва NPC (Шериф: `main_npc`,
      `can_be_murder_victim: false`) и призраков (dead_wife) в `characters/<id>.json` установлено
      `can_be_murder_victim: false`.
    - [ ] Убедиться, что контекстные NPC, Шериф и призраки не фигурируют как `victim` в `murder_matrix.json` (dead_wife
      может быть только `possible_killer`, не `victim`).
- [ ] **Экипаж корабля (ship_crew)**
    - [ ] Проверить, что Врач (`doctor`) имеет `status: left` в `organizations/ship_crew.json` и
      `characters/doctor.json` (давно списан на берег).

## 5. Финальная проверка логики (Logic Audit)

- [ ] **Принцип «Не любое преступление = убийство»**
    - [ ] Проверить, что обнаружение контрабанды/культа/пьянства не автоматически обвиняет в текущем убийстве.
    - [ ] Убедиться, что в `evidence.json` для таких улик указано, чего они **НЕ** доказывают.
- [ ] **Логика детектива**
    - [ ] Проверить, что `investigation` Детектива не противоречит выбранному нанимателю.
    - [ ] Убедиться, что `false_beliefs` Детектива не блокируют его способность расследовать текущее убийство.

## 6. Публикация

- [ ] Обновить `README.md` с финальными решениями (убрать раздел «Незавершённые элементы»).
- [ ] Проверить, что все JSON-файлы валидны и согласованы.
- [ ] Сгенерировать финальную версию сценария (если используется генератор).

## 7. Хронология, таймер, новые локации (внесено в сессию)

> Набросок пролога 14:00–07:15 + таймер потопа + 6 путей решения.

### 7.1. Что уже сделано

- [x] **Фазы `events.json` `project.timeline`**: расширено из одной фазы `prologue` в
  `prologue / chapter_1_city / chapter_2_tavern / chapter_3_night / chapter_4_murder / chapter_5_dawn / chapter_6_flood / finale`.
- [x] **События пролога (14:00–19:00)**: добавлены `event_022`…`event_029` (город, Шериф, морг, дорога, прибытие,
  Художник, конфликт Доктора/Трактирщика, ливень).
- [x] **Таймер потопа**: `event_030` (07:15 — река вышла из берегов) + `flood_mechanic.json` (24 игровых часа, вода
  прибывает снизу вверх, 6 путей решения, 4 концовки).
- [x] **Ежечасные «живые» события**: `nightly_beats.json` (NPC живут своей жизнью по часам: едят, молятся, грузят,
  убивают, общаются).
- [x] **Новые локации**: `kitchen` (обязательна — сцена 18:15–18:30), `morgue`, `dock` (пристань), `sheriff_office` →
  `gameplay`.
- [x] **Новый NPC**: `doctor_assistant` (ассистент Доктора, context_npc, встречает в морге).
- [x] **Принцип «убийство ≠ потоп»**: зафиксировано в `flood_mechanic.separation_of_concerns` (разгадать убийство и
  остановить потоп — две разные задачи, чтобы проклятие Официантки не выдавало её как убийцу автоматически).

### 7.2. Что ОСТАЛСЯ (потому что не удалось сделать сейчас / требует решения автора)

- [x] **Переименование `attic` → `mansard`** (выполнено: locations/attic.json → locations/mansard.json, id attic →
  mansard, Чердак/чердак → Мансарда/мансарда в characters/artist.json, events.json, evidence.json, nightly_beats.json,
  kill_grid.json, flood_mechanic.json, locations/tavern.json, locations/tavern_hall.json, README.md). Проверено: нет
  оставшихся `attic`/`чердак`.
- [x] **Точная привязка таймера к сценам**: добавлено `flood_mechanic.scene_binding` — 1 игровой час = 1 завершённая
  сцена (beat), таймер тикает по `nightly_beats.json`, темп = темп Мастера. Есть `scene_to_hour_map` (game_hour →
  scene → phase) + `acceleration`.
- [x] **Расхождение dead_wife morning vs waitress morning**: решено — в одном morning-слоте dead_wife раньше (приходит в
  waitress_room, event_015), waitress позже (уходит в cult_lair). Обновлены `dead_wife.daily_schedule.morning`,
  `waitress.daily_schedule.morning`, `nightly_beats.json` chapter_5.
- [x] **Принцип «убийство ≠ потоп» в дед-уликах**: проверено — evidence_009/010/027/028 уже имеют
  `does_not_prove: ["Официантка совершила убийство/текущее убийство"]` + `false_leads`. Никакая из 4 улик потопа не
  доказывает автоматически, что Официантка — убийца. (Проверка пройдена, менять не пришлось.)
- [x] **Художник = потомок аасимара/эльфа**: добавлено в `artist.appearance` (face, build, distinguishing_marks) +
  `artist.senses` (smell, voice, behaviors). Намёк на происхождение (event_027, 18:00), НЕ дед-улика.
- [x] **Расширение каста**: `doctor_assistant` + новые локации (morgue, sheriff_office, dock) добавлены в README (раздел
  «Контекстные персонажи» → «Дополнительные NPC и локации», раздел «Контекст и игровое пространство» →
  Context/Gameplay/Top-level). `sheriff_office` убран из Context-примеров (теперь gameplay).
- [x] **Привязка `event_030` к расписанию**: `event_030` (07:15) привязан к `daily_schedule.morning` — срабатывает,
  когда NPC по утренней смене попадают в локации, где видят воду (tavern_hall/tavern_yard/dock). Обновлены
  `event_030.facts`/`consequences`, `chapter_5_dawn`-фаза, `nightly_beats.json` chapter_5 (beat с `event: event_030`).
- [x] **Шериф context_npc → main_npc**: `sheriff.status` → `main_npc`, `can_be_murder_victim: false` (остался, d6 не
  тронуты). README + `characters.schema.json` обновлены (модель `status.main_npc` = «активный NPC», не строго «6
  d6-жертв»).

### 7.3. Что НЕ трогал (требует решения автора, см. прошлый разговор)

- [ ] **Таймер 24 часа — реальное or сжатое время**: сейчас `flood_mechanic.timer` = игровые часы, но не привязано к
  реальному. Решает автор.
- [ ] **Официантка снимает проклятие?** (набросок пользователя): НЕ сделано. Вместо этого — 6 путей решения (см.
  `flood_mechanic.resolution_paths`), чтобы не выдать Официантку как убийцу автоматически.